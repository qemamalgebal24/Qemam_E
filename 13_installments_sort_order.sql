-- ============================================================================
-- إضافة عمود sort_order لجدول الالتزامات (السلف والمخالصات) — الواجهة
-- العامة لإضافة/تعديل الصفوف في index.html محتاجاه زي باقي الجداول
-- (الإقامات، العهدة، سيارات الشركة... إلخ)
-- شغّلي هذا الملف بعد 12_installment_deductions.sql
-- ============================================================================

alter table public.employee_installments
  add column if not exists sort_order bigint default 0;

-- ============================================================================
-- تم!
-- ============================================================================
